"""Adapter abstractions — namespace for the future capability adapter contracts.

The Foundation does **not** ship concrete adapters for external services
(LLMs, vector stores, ...). Those land in their own engineering packages.
This package only defines the *shape* every adapter must conform to.
"""
